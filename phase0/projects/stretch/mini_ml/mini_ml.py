"""
Stretch 4 — Mini ML Framework.

Pure-Python, no NumPy, deliberately skips real backprop. Optional and
lowest priority — doesn't complete the learning loop (no real gradients).
See docs/PHASE0_PROJECTS.md for the full spec.
"""
