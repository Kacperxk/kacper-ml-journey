"""
Stretch 1 — Data Preprocessing Engine.

StandardScaler, MinMaxScaler, OneHotEncoder, train_test_split — all from
scratch in NumPy. Only pursue if ahead of schedule; reasonable to fold
directly into linear_regression's data-prep step instead of building
standalone. See docs/PHASE0_PROJECTS.md for the full spec.
"""
