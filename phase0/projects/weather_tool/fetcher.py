"""
Project 1 — CLI Weather Tool: WeatherFetcher class.

Fetches real weather data from the Open-Meteo API (free, no key needed).
Should raise APIError / ParseError (see exceptions.py) on failure, and log
instead of print. See docs/PHASE0_PROJECTS.md for the full spec.
"""
